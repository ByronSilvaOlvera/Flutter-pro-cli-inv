
part of '../widgets.dart';

class ModelsS extends StatelessWidget {
  const ModelsS({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('00000'),
    );
  }
}